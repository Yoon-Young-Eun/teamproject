<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="CustomerVO">

	<select id="getMember" resultType="customer"
		parameterType="customer">
<<<<<<< HEAD
		SELECT * FROM customer where customer_id =#{customer_id} and customer_passwd=#{customer_passwd}
=======
		SELECT * FROM customer where customer_id=#{customer_id} and customer_passwd=#{customer_passwd}
>>>>>>> main
	</select>

	<insert id="insertMember" parameterType="customer" >
		insert into customer (customer_name, customer_passwd, customer_no, customer_phone, customer_zipcode, customer_address1, customer_address2,customer_birth) 
		values ( #{customer_name}, #{customer_passwd}, #{customer_no}, #{customer_phone}, #{customer_zipcode}, #{customer_address1}, #{customer_address2}, #{customer_birth})

	</insert>

	<select id="idCheck" parameterType="String" resultType="int">
<<<<<<< HEAD
		SELECT COUNT(*) FROM customer where customer_id = #{customer_id}
=======
		SELECT COUNT(*) FROM customer where customer_no = #{customer_no}
	</select>
	<select id="getReadPhoneNum" resultType="customer">
	<![CDATA[ select * from customer where customer_no=#{customer_no} ]]>
	</select>
	
	
	<!-- 내정보수정,탈퇴 -->
	<select id="getCustomerInfo" parameterType="customer" resultType="customer">
		<![CDATA[ select * from customer where customer_no=#{customer_no} ]]>
	</select>
	
	<update id="updateCustomerInfo" parameterType="customer">
		update customer set customer_passwd=#{customer_passwd}, customer_phone=#{customer_phone} where customer_no=#{customer_no}
	</update>
	
	<delete id="deleteCustomer" parameterType="customer">
		delete from customer where customer_no=#{customer_no}
	</delete>
	
	<select id="pwCheck" parameterType="customer" resultType="int">
		select count(*) from customer where customer_passwd=#{customer_passwd} and customer_no=#{customer_no}
>>>>>>> main
	</select>
	
</mapper>

