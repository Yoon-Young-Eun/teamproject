<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="OrderDAO">

	<select id="getArticleCount" parameterType="paging" resultType="int">
	<if test="customer_no != 0">
        	     <![CDATA[ select count(*) from (select @rownum := @rownum+1 as rnum, order_mt_no, customer_no ,customer_name, product_code, order_mt_category1, order_mt_category2, order_mt_product, order_mt_price, order_mt_date, order_no from cm_order, (select @rownum :=0) as rowcolum where]]>
			
				<if test="searchCondition=='order_mt_product'">
    			<![CDATA[order_mt_product like concat('%', #{searchKeyword}, '%') and]]></if>
				<if test="searchCondition=='customer_no'">	
    			<![CDATA[customer_no like concat('%', #{searchKeyword}, '%') and]]></if>
				
   			<![CDATA[customer_no = #{customer_no} order by customer_no desc) as rownum_table]]>
		</if>

		<if test="customer_no ==0">
        	     <![CDATA[ select count(*) from (select @rownum := @rownum+1 as rnum, order_mt_no, customer_no ,customer_name, product_code, order_mt_category1, order_mt_category2, order_mt_product, order_mt_price, order_mt_date, order_no from cm_order, (select @rownum :=0) as rowcolum ]]>
			<trim prefix="where" >
				<if test="searchCondition=='order_mt_product'">
    			<![CDATA[order_mt_product like concat('%', #{searchKeyword}, '%') ]]></if>
				<if test="searchCondition=='customer_no'">	
    			<![CDATA[customer_no like concat('%', #{searchKeyword}, '%') ]]></if>
			</trim>
    <![CDATA[order by customer_no desc) as rownum_table]]></if>
	</select>



	<select id="getUserOrderList" parameterType="paging"
		resultType="cm_order">

		<if test="customer_no != 0">
        	     <![CDATA[ select * from (select @rownum := @rownum+1 as rnum, order_mt_no, customer_no ,customer_name, product_code, order_mt_category1, order_mt_category2, order_mt_product, order_mt_price, order_mt_date, order_no from cm_order, (select @rownum :=0) as rowcolum where]]>
			
				<if test="searchCondition=='order_mt_product'">
    			<![CDATA[order_mt_product like concat('%', #{searchKeyword}, '%') and]]></if>
				<if test="searchCondition=='customer_no'">	
    			<![CDATA[customer_no like concat('%', #{searchKeyword}, '%') and]]></if>
				
   			<![CDATA[customer_no = #{customer_no} order by customer_no desc) as rownum_table where rnum >=#{startRow} and rnum <=#{endRow} ]]>
		</if>

		<if test="customer_no ==0">
        	     <![CDATA[ select * from (select @rownum := @rownum+1 as rnum, order_mt_no, customer_no ,customer_name, product_code, order_mt_category1, order_mt_category2, order_mt_product, order_mt_price, order_mt_date, order_no from cm_order, (select @rownum :=0) as rowcolum ]]>
			<trim prefix="where" >
				<if test="searchCondition=='order_mt_product'">
    			<![CDATA[order_mt_product like concat('%', #{searchKeyword}, '%') ]]></if>
				<if test="searchCondition=='customer_no'">	
    			<![CDATA[customer_no like concat('%', #{searchKeyword}, '%') ]]></if>
			</trim>
    <![CDATA[order by customer_no desc) as rownum_table where rnum >=#{startRow} and rnum <=#{endRow} ]]></if>
	</select>

</mapper>