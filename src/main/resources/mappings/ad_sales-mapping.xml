<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="SalesDAO">

    <!-- 매출 테이블 카운트(검색조건 없음)-->
	<select id="getArticleCount"  resultType="int">
		select count(*) from order_cm
	</select>


    <!-- 매출 조회를 위한 order_cm 테이블 페이징 쿼리-->
	<select id="getStoreSalesList" parameterType="paging" resultType="order">
        <![CDATA[ select * from (select @rownum := @rownum+1 as rnum, order_no, order_type, order_product, store_code, order_date, order_pickup_date, cm_gate_passwd, order_request1, order_request2, order_price, order_use_coupon_price, order_status, order_price_method, customer_no, order_customer_name, order_store_name, order_expected_date, order_customer_phone, order_address1, order_address2, order_delivery_price from order_cm, (select @rownum :=0) as rowcolum order by order_no desc) as rownum_table where rnum >=#{startRow} and rnum <=#{endRow} ]]>
	</select>

    <!-- 매출 조회를 위한 order_cm 테이블 페이징 쿼리-->
	<select id="getProductSalesList" parameterType="paging" resultType="orderManagement">
        <![CDATA[ select * from (select @rownum := @rownum+1 as rnum, customer_no, customer_name, product_code, order_mt_category1, order_mt_category2, order_mt_product, order_mt_price, order_mt_date, order_no from order_management, (select @rownum :=0) as rowcolum order by order_no desc) as rownum_table where rnum >=#{startRow} and rnum <=#{endRow} ]]>
	</select>

   
  

</mapper>