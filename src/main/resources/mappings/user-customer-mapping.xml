<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="CustomerVO">

	<select id="getMember" resultType="customer"
		parameterType="customer">
		SELECT * FROM CUSTOMER where customer_id =#{customer_id} and
		customer_passwd=#{customer_passwd}
	</select>

	<insert id="insertMember" parameterType="customer" >
		insert into customer (customer_name, customer_passwd, customer_id, customer_phone, customer_zipcode, customer_address1, customer_address2,customer_birth) 
		values ( #{customer_name}, #{customer_passwd}, #{customer_id}, #{customer_phone}, #{customer_zipcode}, #{customer_address1}, #{customer_address2}, #{customer_birth})

	</insert>

	<select id="idCheck" parameterType="String" resultType="int">
		SELECT COUNT(*) FROM CUSTOMER where customer_id = #{customer_id}
	</select>
</mapper>

